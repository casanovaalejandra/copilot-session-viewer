[project]
name = "copilot-session-viewer"
version = "1.0.0"
description = "Browse and manage your GitHub Copilot CLI conversation history"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "github-copilot-sdk>=0.1.32",
]